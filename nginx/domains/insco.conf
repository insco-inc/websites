# 序号	  访问URL	                   location配置	  proxy_pass配置	       后端接收的请求
# 1	    test.com/user/test.html	   /user/	        http://test1/	       /test.html
# 2	    test.com/user/test.html	   /user/	        http://test1	       /user/test.html
# 3	    test.com/user/test.html	   /user	        http://test1	       /user/test.html
# 4	    test.com/user/test.html	   /user	        http://test1/	       //test.html
# 5	    test.com/user/test.html	   /user/	        http://test1/haha/	   /haha/test.html
# 6	    test.com/user/test.html	   /user/	        http://test1/haha	   /hahatest.html

# insco.cn
server {
    listen 80;

    server_name insco.cn www.insco.cn;
    return 301 https://www.insco.cn$request_uri;
}

server {
    listen 443 ssl http2;

    server_name insco.cn;

    ssl_certificate     /certs/insco.cn.pem;
    ssl_certificate_key /certs/insco.cn.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    return 301 https://www.insco.cn$request_uri;
}

server {
    listen 443 ssl http2;

    server_name www.insco.cn;

    ssl_certificate     /certs/insco.cn.pem;
    ssl_certificate_key /certs/insco.cn.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    add_header Access-Control-Allow-Origin *;

    location / {
        proxy_pass         http://web:3000/;
        proxy_set_header   Accept-Language        zh;
        proxy_set_header   Custom-Real-Source     cn;
        proxy_set_header   Custom-Forwarded-Host  $host;
        proxy_set_header   X-Real-IP              $remote_addr;
        proxy_set_header   X-Forwarded-For        $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto      $http_x_forwarded_proto;
    }
}

# insco.com.cn
server {
    listen 80;

    server_name insco.com.cn www.insco.com.cn;
    return 301 https://www.insco.com.cn$request_uri;
}

server {
    listen 443 ssl http2;

    server_name insco.com.cn;

    ssl_certificate     /certs/insco.com.cn.pem;
    ssl_certificate_key /certs/insco.com.cn.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    return 301 https://www.insco.com.cn$request_uri;
}

server {
    listen 443 ssl http2;

    server_name www.insco.com.cn;

    ssl_certificate     /certs/insco.com.cn.pem;
    ssl_certificate_key /certs/insco.com.cn.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    add_header Access-Control-Allow-Origin *;

    location / {
        proxy_pass         http://web:3000/;
        proxy_set_header   Accept-Language        zh;
        proxy_set_header   Custom-Real-Source     com_cn;
        proxy_set_header   Custom-Forwarded-Host  $host;
        proxy_set_header   X-Real-IP              $remote_addr;
        proxy_set_header   X-Forwarded-For        $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto      $http_x_forwarded_proto;
    }
}

# insco.io
server {
    listen 80;

    server_name insco.io www.insco.io;
    return 301 https://www.insco.io$request_uri;
}

server {
    listen 443 ssl http2;

    server_name insco.io;

    ssl_certificate     /certs/insco.io.pem;
    ssl_certificate_key /certs/insco.io.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    return 301 https://www.insco.io$request_uri;
}

server {
    listen 443 ssl http2;

    server_name www.insco.io;

    ssl_certificate     /certs/insco.io.pem;
    ssl_certificate_key /certs/insco.io.key;

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4:!DH:!DHE;
    ssl_prefer_server_ciphers on;
    ssl_session_timeout  5m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    add_header Access-Control-Allow-Origin *;

    location / {
        proxy_pass         http://web:3000/;
        proxy_set_header   Custom-Forwarded-Host  $host;
        proxy_set_header   X-Real-IP              $remote_addr;
        proxy_set_header   X-Forwarded-For        $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto      $http_x_forwarded_proto;
    }
}
